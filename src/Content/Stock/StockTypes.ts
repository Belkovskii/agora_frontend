export interface Stock {
    id : string,
    name : string,
    address : string,
    contact : string
}

export interface Item {
    id : string,
    name : string,
    measurementUnit : string,
    createdAt : Date
}